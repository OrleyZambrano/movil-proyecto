// Cambia solo la IP aquí
const API_HOST = '10.136.23.84';
const API_PORT = '8000';

export const API_URL = `http://${API_HOST}:${API_PORT}/api`;
export const API_BASE = `http://${API_HOST}:${API_PORT}`;
